/*import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  users: [],
  totalUsers: 0,
  isLoading: true,
};

const getUsersSlice = createSlice({
  name: "getUsers",
  initialState,
});

//console.log(getUsersSlice);
export default getUsersSlice.reducer;*/
